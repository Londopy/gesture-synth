# Frontend (Vite): community service base URL baked into the build
VITE_COMMUNITY_URL=http://localhost:8787

# Community service (services/community)
PORT=8787
# DATABASE_URL=postgres://user:pass@localhost:5432/gesture_synth   # unset = in-memory store
FRONTEND_ORIGIN=http://localhost:5173
CORS_ORIGIN=*
SECRET_KEY_BASE=change-me
